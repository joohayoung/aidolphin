var wavesurfer = WaveSurfer.create({
    // 필수 옵션
    container: '#waveform',
    // 선택 옵션들 
    waveColor: 'violet',
    progressColor: 'purple'
});
// <!-- wave객체에 있는 load()함수를 호출-->

// wavesurfer.load(music.fname);
// window.onload = function() {
//     var wavesurfer = WaveSurfer.create({
//         // 필수 옵션
//         container: '#waveform',
//         // 선택 옵션들 
//         waveColor: 'violet',
//         progressColor: 'purple'
//         });
//         // <!-- wave객체에 있는 load()함수를 호출-->

//     // wavesurfer.load(music.fname);
// }